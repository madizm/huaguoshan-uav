# Non-secret PostgREST settings for local development.
#
# Provide connection credentials and JWT secret through environment-managed
# values rather than committing them here:
#   PGRST_DB_URI=postgres://postgrest_authenticator:<secret>@10.1.109.151:5432/huaguoshan_projd
#   PGRST_JWT_SECRET=<HS256 secret shared with auth adapter>
# See pgrest.env.example.

db-schemas = "citydb, public, terrain, airspace"
db-anon-role = "web_anon"
server-port = 13000
